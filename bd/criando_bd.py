import sqlite3

# criando banco de dados, voce cria uma conexao, e apos isso voce apenas usa o camando do modulo sqlite3.
# EX: sqlite3.connect('nome_do_bando_de_dados.db') db(data bank)
conexao = sqlite3.connect('titulo.db')